{
  "name": "cardoon",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "install-server": "cd server && yarn install",
    "start-server-with-build": "cd server && yarn build && yarn start",
    "start-server": "cd server && yarn start",
    "test-server": "cd server && yarn test",
    "test-server:coverage": "cd server && yarn test:coverage:report",
    "test-server:watch": "cd server && yarn test:watch",
    "install-client": "cd Cardoon && yarn",
    "build-client": "cd Cardoon && yarn build",
    "start-client": "cd Cardoon && yarn start",
    "test:all": "yarn test-server && cd Cardoon && yarn test --run",
    "test:coverage": "yarn test-server:coverage && cd Cardoon && yarn test --run --coverage",
    "test:coverage:open": "yarn test-server:coverage && cd Cardoon && yarn test --run --coverage && echo '🔍 Opening coverage reports...' && (cd server && yarn serve coverage) & (cd Cardoon && yarn serve coverage)",
    "pre-commit": ".husky/pre-commit",
    "pre-push": ".husky/pre-push"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "huggingface": "^1.4.0",
    "motion": "^12.23.24",
    "serve": "^14.2.4"
  },
  "devDependencies": {
    "@types/node": "^22.14.0"
  }
}
