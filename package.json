{
  "name": "cardoon",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "install-server": "cd server && npm install",
    "start-server-with-build": "cd server && npm run build && npm run start",
    "start-server": "cd server && npm run start",
    "test-server": "cd server && npm run test",
    "test-server:coverage": "cd server && npm run test:coverage:report",
    "test-server:watch": "cd server && npm run test:watch",
    "install-client": "cd Cardoon && yarn",
    "build-client": "cd Cardoon && yarn build",
    "start-client": "cd Cardoon && yarn start",
    "test:all": "npm run test-server && cd Cardoon && yarn test --run",
    "test:coverage": "npm run test-server:coverage && cd Cardoon && yarn test --run --coverage",
    "test:coverage:open": "npm run test-server:coverage && cd Cardoon && yarn test --run --coverage && echo '🔍 Opening coverage reports...' && (cd server && npx serve coverage) & (cd Cardoon && npx serve coverage)",
    "pre-commit": ".husky/pre-commit",
    "pre-push": ".husky/pre-push"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "huggingface": "^1.4.0",
    "motion": "^12.23.24",
    "serve": "^14.2.4"
  },
  "devDependencies": {
    "@types/node": "^22.14.0"
  }
}
